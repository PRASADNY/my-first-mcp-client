# MCP Client Authentication Configuration
# These credentials are used to authenticate with the MCP server

# Option 1: API Key Authentication
# Uncomment and set your API key to use API key authentication
# MCP_API_KEY=your-api-key-here

# Option 2: Bearer Token Authentication
# Uncomment and set your token to use Bearer token authentication
# You can use either MCP_TOKEN or MCP_BEARER_TOKEN
# MCP_TOKEN=your-bearer-token-here
# MCP_BEARER_TOKEN=your-bearer-token-here

# Example (matching server default):
MCP_API_KEY=default-api-key-12345

# Note: If both API key and token are provided, API key takes precedence
# If neither is provided, the client will connect without authentication
# (this works only if the server has MCP_AUTH_REQUIRED=false)
